# How to run

Once you define your info in `experiments.yaml` you run

`python run.py --run conservative_nitrogen high_uncertainty_fire`

and then compare with

`python compare.py conservative_nitrogen high_uncertainty_fire --var γLT m`
